---
phase: 14-integration-gpu-benchmark
plan: "14.1-06"
type: execute
wave: 5
depends_on: ["14.1-01", "14.1-02", "14.1-03", "14.1-04", "14.1-05"]
files_modified:
  - .planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "14-VERIFICATION.md frontmatter status changes from `gaps_found` to `complete` (or `complete_with_notes` if VALID-03 failed but Phase 14's code is verified correct)"
    - "Each originally-failed success criterion (SC3, SC4, SC5) has an updated row in the Observable Truths table pointing to the 14.1-NN-SUMMARY.md that closed it"
    - "Each originally human-needed test has an updated status (verified or documented failure) with reference to the plan that produced the evidence"
    - "Phase 15 unblocked: a concluding paragraph identifies which Benchmark decision gate row (sampler=blackjax from 14.1-03) Phase 15 should consume and what partition/walltime that implies"
  artifacts:
    - path: ".planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md"
      provides: "Updated verification report reflecting 14.1 gap-closure outcomes"
      contains: "status: complete"
  key_links:
    - from: "14-VERIFICATION.md"
      to: "14.1-02-SUMMARY.md, 14.1-03-SUMMARY.md, 14.1-04-SUMMARY.md, 14.1-05-SUMMARY.md"
      via: "Evidence links for each closed gap"
      pattern: "14.1-"
---

<objective>
Re-verify Phase 14 after gap-closure plans 14.1-01 through 14.1-05 have produced their artifacts. Update .planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md to reflect the new evidence. Flip `status: gaps_found` to `status: complete` (or `complete_with_notes` if VALID-03 failed) so /gsd:discuss-phase 15 is unblocked.

This is a pure documentation/metadata plan — no code changes, no cluster interaction. It reads the 14.1 SUMMARYs and rewrites the verification frontmatter + Observable Truths table rows for SC3, SC4, SC5 and the two human-needed tests.

Purpose: Without this re-verification, `status: gaps_found` in 14-VERIFICATION.md continues to block downstream phase gates regardless of how many 14.1 plans closed evidence.

Output: Updated 14-VERIFICATION.md with current status, evidence links, and a Phase 15 readiness paragraph.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
@.planning/phases/14-integration-gpu-benchmark/14.1-02-SUMMARY.md
@.planning/phases/14-integration-gpu-benchmark/14.1-03-SUMMARY.md
@.planning/phases/14-integration-gpu-benchmark/14.1-04-SUMMARY.md
@.planning/phases/14-integration-gpu-benchmark/14.1-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all 14.1 SUMMARYs and compose an updated VERIFICATION.md</name>
  <files>.planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md</files>
  <action>
  Step 1 — Read evidence:
    - .planning/phases/14-integration-gpu-benchmark/14.1-02-SUMMARY.md (numpyro benchmark decision + gpu_hours_per_chunk)
    - .planning/phases/14-integration-gpu-benchmark/14.1-03-SUMMARY.md (blackjax benchmark decision + gpu_hours_per_chunk, numpyro-vs-blackjax comparison)
    - .planning/phases/14-integration-gpu-benchmark/14.1-04-SUMMARY.md (VALID-03 verdict: PASSED or FAILED + per-parameter relative errors)
    - .planning/phases/14-integration-gpu-benchmark/14.1-05-SUMMARY.md (BENCH-05 cross-submission JIT verdict)

  Step 2 — Rewrite 14-VERIFICATION.md frontmatter:
    - `verified`: change to today's date (2026-04-18 or later if executed later)
    - `status`: set to `complete` if SC3, SC4, SC5 all PASSED; set to `complete_with_notes` if SC5 (VALID-03) FAILED but the code is verified correct (the failure is a numerics issue to investigate, not a Phase 14 blocker); set to `gaps_found` ONLY if some gap is still open (in which case call that out explicitly).
    - `score`: update from "2/5 success criteria fully verified (3 require GPU cluster execution)" to "5/5 success criteria verified" (or "4/5 verified; SC5 FAILED" as applicable).
    - `gaps`: empty the list (or leave only truly-unresolved ones).
    - `human_verification`: mark each test as verified with a pointer to the closing plan.

  Step 3 — Rewrite the Observable Truths table (lines ~50-56):
    - Truth 3 (benchmark_batched.json): change FAILED to VERIFIED; evidence: "BlackJAX run from 14.1-03 wrote results/power/benchmark_batched.json with all 10 schema fields; numpyro baseline archived at results/power/benchmark_batched_numpyro.json (14.1-02)".
    - Truth 4 (STATE.md decision gate row): change FAILED to VERIFIED; evidence: "Two rows: sampler=numpyro from 14.1-02, sampler=blackjax from 14.1-03".
    - Truth 5 (VALID-03): change HUMAN NEEDED to VERIFIED or FAILED per 14.1-04 verdict; evidence: "cluster/14_valid03_cpu.slurm + cluster/14_valid03_gpu.slurm ran on M3; validation/valid03_cross_platform.py compare --rtol 0.01 returned <exit code>" + max relative error per parameter.

  Step 4 — Rewrite the Human Verification Required section (lines ~107-131):
    - Test 1 (GPU benchmark execution): mark VERIFIED; link to 14.1-02 and 14.1-03 SUMMARYs.
    - Test 2 (cross-chunk JIT cache): mark VERIFIED or FAILED per 14.1-05 verdict; link to 14.1-05 SUMMARY.
    - Test 3 (VALID-03 cross-platform): mark VERIFIED or FAILED per 14.1-04 verdict; link to 14.1-04 SUMMARY.

  Step 5 — Add a Phase 15 Readiness section at the end of the document:
    - Which benchmark result drives Phase 15: the BlackJAX row (from 14.1-03) because that is the production sampler.
    - Decision: gpu or cpu_comp (read from SUMMARY).
    - gpu_hours_per_chunk value.
    - Recommended Phase 15 SLURM partition and walltime based on the decision.
    - Note on whether JIT gate thresholds in iteration.py apply_decision_gate should be tightened (STATE.md decision 105); tightening belongs in Phase 15 planning, not this re-verification.

  Step 6 — Commit:
    git add .planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
    git commit -m "docs(14.1-06): re-verify Phase 14 after gap-closure"
    git push origin main
  </action>
  <verify>
  1. `grep "^status:" .planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md` shows `complete` or `complete_with_notes` (not `gaps_found`).
  2. `grep "^score:" .planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md` shows a number >= 4/5.
  3. VERIFICATION.md references all four 14.1-NN-SUMMARY.md filenames.
  4. VERIFICATION.md contains a Phase 15 Readiness section with the production sampler's decision and gpu_hours_per_chunk.
  5. `git log --oneline -1 -- .planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md` shows the re-verification commit.
  </verify>
  <done>
  14-VERIFICATION.md updated with all 14.1 evidence linked; status flipped away from `gaps_found`; Phase 15 readiness paragraph identifies the authoritative decision-gate result.
  </done>
</task>

</tasks>

<verification>
1. VERIFICATION.md frontmatter status reflects the true state after 14.1 gap-closure.
2. All five Observable Truths rows are VERIFIED or explicitly marked FAILED (with reason).
3. All three human_verification tests are resolved.
4. Phase 15 has the single authoritative data point it needs (BlackJAX decision + gpu_hours_per_chunk).
</verification>

<success_criteria>
- Phase 14 verification status no longer blocks /gsd:discuss-phase 15.
- Every 14.1 SUMMARY is referenced from the updated VERIFICATION.md.
- Phase 15 planning can begin.
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-06-SUMMARY.md` with:
- Final Phase 14 verification status
- Count of VERIFIED vs FAILED truths
- Link to each 14.1 SUMMARY
- One-sentence Phase 15 go/no-go recommendation
</output>
