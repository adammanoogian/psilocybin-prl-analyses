---
phase: 14-integration-gpu-benchmark
plan: "14.1-02"
type: execute
wave: 2
depends_on: ["14.1-01"]
files_modified:
  - results/power/benchmark_batched.json
  - .planning/STATE.md
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "results/power/benchmark_batched.json exists on the local machine (synced from cluster via git pull) and contains all Phase 14 schema fields: per_iteration_s, peak_vram_mb, mean_gpu_util_pct, decision, gpu_hours_per_chunk, jit_cold_s, jit_warm_s, benchmark_n_participant_sessions, grid, mcmc_settings"
    - "The in-flight numpyro benchmark job has terminated (completed successfully OR hit the 8h walltime OR failed); its cluster/logs/bench14_${SLURM_JOB_ID}.out/.err are retrievable"
    - ".planning/STATE.md Key Decisions table contains a Benchmark decision gate row with the numpyro run's per_iter_s, gpu_hours_per_chunk, and decision (gpu or cpu_comp) — written either by _update_state_md on the cluster OR manually by this plan if the cluster-side append failed"
    - "The commit that adds benchmark_batched.json + STATE.md row is pushed to origin/main so both local and cluster filesystems are in sync"
  artifacts:
    - path: "results/power/benchmark_batched.json"
      provides: "Numpyro-baseline Phase 14 benchmark output"
      contains: "per_iteration_s"
    - path: ".planning/STATE.md"
      provides: "Benchmark decision gate row for sampler=numpyro"
      contains: "Benchmark decision gate"
  key_links:
    - from: "results/power/benchmark_batched.json"
      to: ".planning/STATE.md"
      via: "Decision gate row records decision + gpu_hours_per_chunk from benchmark JSON"
      pattern: "Benchmark decision gate"
---

<objective>
Triage the numpyro GPU benchmark job that was running on M3 at plan time. When it finishes (or walltimes out), land its results on the local machine and ensure STATE.md has a Benchmark decision-gate row. This is the "receive and verify" counterpart to the cluster-side submission.

Because the numpyro SLURM script in flight does NOT have the git auto-push block (plan 14.1-01 adds it for future submissions, but the currently-running job already loaded the unpatched script), results will land on M3 filesystem only. This plan handles the cluster-to-local transfer manually.

Purpose: Gap closure for VERIFICATION.md SC3 and SC4 (benchmark_batched.json missing, STATE.md decision-gate row missing). The numpyro run closes both gaps for the numpyro-era thresholds; plan 14.1-03 then re-runs with BlackJAX for the production-era thresholds.

Output: results/power/benchmark_batched.json present locally; STATE.md contains a "Benchmark decision gate (sampler=numpyro)" row in Key Decisions; both committed and pushed to origin/main.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
@cluster/14_benchmark_gpu.slurm
@scripts/08_run_power_iteration.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm in-flight numpyro benchmark has terminated and retrieve results</name>
  <what-built>
  The SLURM job submitted before plan-time with `--sampler numpyro` (hard-coded in the unpatched cluster/14_benchmark_gpu.slurm). Walltime is --time=08:00:00; at plan-time elapsed was ~5:45; the job will either complete (writing results/power/benchmark_batched.json on the cluster AND calling _update_state_md to append a STATE.md row) OR walltime-fail at 8:00.
  </what-built>
  <how-to-verify>
  On M3:
    1. `squeue -u $USER -n prl_bench14` — confirm job is no longer in R or PD state.
    2. `sacct -j <JOB_ID> --format=JobID,State,ExitCode,Elapsed` — look for COMPLETED, TIMEOUT, or FAILED.
    3. `tail -50 cluster/logs/bench14_<JOB_ID>.out` — look for "BENCHMARK COMPLETE" (success) or "BENCHMARK FAILED" / walltime-killed banner.
    4. If COMPLETED and benchmark_batched.json exists on cluster:
       - `cd ~/psilocybin_prl_analyses` (or wherever the repo lives on M3)
       - `git status` — expect results/power/benchmark_batched.json AND possibly .planning/STATE.md as modified/untracked
       - `git add results/power/benchmark_batched.json .planning/STATE.md cluster/logs/bench14_<JOB_ID>.out cluster/logs/bench14_<JOB_ID>.err`
       - `git commit -m "bench phase14 gpu sampler=numpyro (job <JOB_ID>)"`
       - `git push origin main`
    5. If TIMEOUT or FAILED: collect cluster/logs/bench14_<JOB_ID>.err, paste contents into the resume signal so Claude can diagnose.

  On local machine after push:
    6. `git pull origin main`
    7. `ls results/power/benchmark_batched.json` — must exist.
    8. `python -m json.tool results/power/benchmark_batched.json` — must be valid JSON with keys per_iteration_s, peak_vram_mb, mean_gpu_util_pct, decision, gpu_hours_per_chunk, jit_cold_s, jit_warm_s.
  </how-to-verify>
  <resume-signal>
  Paste one of:
    - "completed; pushed job <JOB_ID>, benchmark_batched.json visible locally"
    - "timed out; err log contents: <paste>"
    - "failed; err log contents: <paste>"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify schema and backfill STATE.md row if cluster-side _update_state_md failed</name>
  <files>.planning/STATE.md</files>
  <action>
  Assumes Task 1 is resolved with a local copy of results/power/benchmark_batched.json.

  Step 1 — Schema verification:
  Read results/power/benchmark_batched.json. Confirm all required Phase 14 schema fields are present and finite:
    - per_iteration_s (float > 0)
    - peak_vram_mb (float >= 0; may be 0 if nvidia-smi sampling failed)
    - mean_gpu_util_pct (float 0-100)
    - decision ("gpu" or "cpu_comp")
    - gpu_hours_per_chunk (float > 0)
    - jit_cold_s (float > 0)
    - jit_warm_s (float >= 0)
    - benchmark_n_participant_sessions (int)
    - grid (dict with n_per_group, max_n, effect_sizes, n_iterations, n_chunks)
    - mcmc_settings (dict with chains, draws, tune, sampler)
  If any required field is missing or non-finite, surface a clear error to the user and STOP — do not fabricate values.

  Step 2 — STATE.md row verification:
  Read .planning/STATE.md. Search for the string "Benchmark decision gate" in the Key Decisions table.
    - If found: confirm its per_iter_s, gpu_hours_per_chunk, and decision match the JSON. If they do, this task is complete — nothing to write.
    - If NOT found (cluster-side _update_state_md failed, e.g. cross-filesystem write or permissions): manually append a row to the Key Decisions table matching the exact format used by scripts/08_run_power_iteration.py _update_state_md (lines 244-302). The row must be:

      `| Benchmark decision gate: <decision> (<gpu_hours_per_chunk> GPU-hrs/chunk) | BENCH-02: per_iter_s=<round(per_iteration_s, 1)>, formula=per_iter_s*600/3600 <op> 50 | 14-02 |`

      where `<op>` is `>` if decision == "cpu_comp" else `<=`.

      Insert BEFORE any section heading that follows the last existing table row (same insertion logic as _update_state_md lines 290-302). Label the row with `sampler=numpyro` so plan 14.1-03 can append a sibling `sampler=blackjax` row without collision — modify the first column to:

      `| Benchmark decision gate sampler=numpyro: <decision> (<gpu_hours_per_chunk> GPU-hrs/chunk) | ... | 14.1-02 |`

      (This deviates from _update_state_md's exact format because we now have TWO samplers to record. Prefer this disambiguated form over the bare "Benchmark decision gate" label.)

  Step 3 — Commit and push:
  If STATE.md was modified, commit with:
    `git add .planning/STATE.md results/power/benchmark_batched.json`
    `git commit -m "docs(14.1): record numpyro benchmark decision gate in STATE.md"`
    `git push origin main`

  If STATE.md was already populated correctly by the cluster job, skip the commit.
  </action>
  <verify>
  1. `grep -c "Benchmark decision gate" .planning/STATE.md` returns >= 1.
  2. `grep "Benchmark decision gate" .planning/STATE.md` shows the row with a numeric gpu_hours_per_chunk, decision value ("gpu" or "cpu_comp"), and phase tag "14-02" or "14.1-02".
  3. `python -c "import json; d=json.load(open('results/power/benchmark_batched.json')); assert all(k in d for k in ['per_iteration_s','peak_vram_mb','mean_gpu_util_pct','decision','gpu_hours_per_chunk','jit_cold_s','jit_warm_s','grid','mcmc_settings']); print('schema OK'); print(d['decision'], d['gpu_hours_per_chunk'])"` exits 0 and prints "schema OK".
  4. `git log --oneline -5` shows either the new docs(14.1) commit OR an earlier cluster-side bench commit from Task 1.
  </verify>
  <done>
  results/power/benchmark_batched.json exists locally and passes schema check; STATE.md Key Decisions table contains a labelled-numpyro Benchmark decision gate row with per_iter_s, gpu_hours_per_chunk, and decision; both artifacts committed to origin/main.
  </done>
</task>

</tasks>

<verification>
1. Phase 14 VERIFICATION.md SC3 (benchmark_batched.json exists) — CLOSED by Task 2.
2. Phase 14 VERIFICATION.md SC4 (STATE.md decision gate row) — CLOSED by Task 2 (labelled `sampler=numpyro`).
3. Git log shows the benchmark JSON committed to origin/main.
4. Schema check passes on the local copy of benchmark_batched.json.
</verification>

<success_criteria>
- SC3: results/power/benchmark_batched.json present locally, valid Phase 14 schema.
- SC4: STATE.md has `Benchmark decision gate sampler=numpyro: ...` row.
- Numpyro baseline numbers recorded; ready for plan 14.1-03 to add BlackJAX sibling row.
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-02-SUMMARY.md` — include the numpyro per_iteration_s, decision, and gpu_hours_per_chunk for comparison against the BlackJAX run in 14.1-03.
</output>
