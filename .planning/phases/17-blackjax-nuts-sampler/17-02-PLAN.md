---
phase: 17-blackjax-nuts-sampler
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - tests/test_hierarchical_logp.py
  - cluster/16_smoke_test_gpu.slurm
  - cluster/08_power_sweep.slurm
autonomous: true

must_haves:
  truths:
    - "A 5-participant fit with BlackJAX produces finite posteriors with correct participant dimensions (VALID-02)"
    - "The BlackJAX log-posterior returns finite scalars and finite gradients for dummy data at prior modes"
    - "ArviZ InferenceData from _samples_to_idata has chain, draw, and participant dims with correct sizes"
    - "Existing VALID-01 bit-exact tests still pass unchanged (PyTensor Op path unaffected)"
    - "The numpyro fallback path is explicitly tested with sampler='numpyro'"
    - "SLURM smoke test exercises BlackJAX by default (via fit_batch_hierarchical)"
    - "VALID-03 (cross-platform CPU/GPU consistency) is deferred to Phase 14 — not tested here"
  artifacts:
    - path: "tests/test_hierarchical_logp.py"
      provides: "VALID-01/02 tests updated for BlackJAX, new BlackJAX-specific smoke tests"
      contains: "blackjax"
    - path: "cluster/16_smoke_test_gpu.slurm"
      provides: "Updated smoke test referencing BlackJAX"
      contains: "BlackJAX"
    - path: "cluster/08_power_sweep.slurm"
      provides: "GPU count appropriate for pmap chain parallelism"
  key_links:
    - from: "test_valid_02_batched_blackjax_convergence"
      to: "fit_batch_hierarchical"
      via: "calls fit_batch_hierarchical with default sampler (blackjax)"
      pattern: "fit_batch_hierarchical.*model_name"
    - from: "test_blackjax_log_posterior_smoke"
      to: "_build_log_posterior"
      via: "calls _build_log_posterior and evaluates logdensity_fn"
      pattern: "_build_log_posterior"
---

<objective>
Update validation tests to exercise the BlackJAX path and update SLURM scripts
for multi-GPU pmap support.  Ensure VALID-01/02 pass with the new sampler and
add BlackJAX-specific smoke tests.

Purpose: Validate that the BlackJAX NUTS implementation produces correct
posteriors (equivalent to numpyro-direct within MCSE) and that cluster scripts
are ready for multi-GPU chain parallelism.

Output: Updated test_hierarchical_logp.py with BlackJAX tests, updated SLURM
scripts.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-blackjax-nuts-sampler/17-RESEARCH.md
@.planning/phases/17-blackjax-nuts-sampler/17-01-SUMMARY.md
@src/prl_hgf/fitting/hierarchical.py
@tests/test_hierarchical_logp.py
@cluster/16_smoke_test_gpu.slurm
@cluster/08_power_sweep.slurm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update validation tests for BlackJAX path</name>
  <files>tests/test_hierarchical_logp.py</files>
  <action>
Update tests/test_hierarchical_logp.py to validate the BlackJAX NUTS path.
The existing VALID-01 tests (P=1 bit-exact, P=2 doubling) test the PyTensor Op
path (build_logp_ops_batched) which is UNCHANGED -- these tests stay as-is.

Add the following new tests:

1. **test_blackjax_log_posterior_smoke** (fast test):
   - Import `_build_log_posterior` and `build_logp_fn_batched` from hierarchical.
   - Build a batched_logp_fn for "hgf_3level" with n_trials=50.
   - Create dummy JAX arrays: input_data (2, 50, 3), observed (2, 50, 3),
     choices (2, 50), trial_mask (2, 50) -- all zeros/ones.
   - Call `_build_log_posterior(batched_logp_fn, input_data, observed, choices,
     trial_mask, n_participants=2, model_name="hgf_3level")`.
   - Evaluate the returned logdensity_fn with a params dict at prior modes:
     `{"omega_2": jnp.full((2,), -3.0), "omega_3": jnp.full((2,), -6.0),
     "kappa": jnp.full((2,), 1.0), "log_beta": jnp.full((2,), 0.0),
     "zeta": jnp.full((2,), 0.0)}`.
   - Assert result is a finite scalar.
   - Also test the 2-level variant with appropriate params dict.

2. **test_blackjax_log_posterior_gradient_smoke** (fast test):
   - Same setup as above but call `jax.grad(logdensity_fn)(params)`.
   - Assert all gradient values are finite (not NaN, not inf).
   - This validates that BlackJAX can differentiate through the nested lax.scan.

3. **Update test_valid_02_batched_blackjax_convergence** (slow test):
   - RENAME the existing `test_valid_02_batched_numpyro_convergence` to
     `test_valid_02_batched_numpyro_convergence` (keep it, it tests numpyro
     fallback path).
   - ADD a new test `test_valid_02_batched_blackjax_convergence` that is
     identical in structure but calls `fit_batch_hierarchical` with the default
     sampler (which is now "blackjax").
   - Same 5-participant fixture, same model_name="hgf_2level", same
     n_chains=2, n_draws=500, n_tune=500.
   - Same checks: posterior has expected variables, participant dimension
     present, finite posteriors, reasonable value bounds.
   - Add `@pytest.mark.slow` marker.
   - NOTE: This test covers VALID-02 for BlackJAX only.  VALID-03
     (cross-platform CPU/GPU consistency) is owned by Phase 14 and not
     tested here.

4. **test_blackjax_samples_to_idata_smoke** (fast test):
   - Import `_samples_to_idata` from hierarchical.
   - Create dummy positions dict: `{"omega_2": np.random.randn(2, 100, 3),
     "log_beta": np.random.randn(2, 100, 3), "zeta": np.random.randn(2, 100, 3)}`
     (shapes: n_chains=2, n_draws=100, P=3).
   - Create dummy sample_stats: `{"diverging": np.zeros((2, 100), dtype=bool),
     "acceptance_rate": np.ones((2, 100)) * 0.95}`.
   - Call `_samples_to_idata(positions, sample_stats, ["omega_2", "log_beta",
     "beta", "zeta"], ["P001", "P002", "P003"], ["placebo"]*3,
     ["baseline"]*3, model_name="hgf_2level")`.
   - Assert: idata has posterior group, "omega_2" in posterior, "beta" in
     posterior (deterministic), participant dim with 3 elements, chain dim
     with 2 elements, draw dim with 100 elements.
   - Assert: "diverging" in sample_stats group.

5. **Keep existing tests unchanged:**
   - test_valid_01_batched_at_p1_bit_exact_3level -- UNCHANGED (tests PyTensor Op)
   - test_valid_01_batched_at_p1_bit_exact_2level -- UNCHANGED
   - test_valid_01_batched_at_p2_doubles_logp -- UNCHANGED
   - test_layer_2_clamping_returns_finite_under_unstable_params -- UNCHANGED
   - test_build_logp_fn_batched_smoke -- UNCHANGED
   - test_numpyro_model_importable -- UNCHANGED

6. **Update the existing numpyro convergence test:**
   - Keep `test_valid_02_batched_numpyro_convergence` but explicitly pass
     `sampler="numpyro"` to ensure it tests the fallback path.  The test
     body stays identical otherwise.
  </action>
  <verify>
  Run `pytest tests/test_hierarchical_logp.py -v -k "not slow" --timeout=120` to verify all fast tests pass.

  Run `pytest tests/test_hierarchical_logp.py -v -k "blackjax_log_posterior_smoke" --timeout=120` to specifically verify the new BlackJAX smoke tests.

  Run `ruff check tests/test_hierarchical_logp.py` for lint compliance.
  </verify>
  <done>
  All fast tests pass (VALID-01 unchanged, new BlackJAX smoke tests pass).
  test_valid_02_batched_numpyro_convergence explicitly uses sampler="numpyro".
  New test_valid_02_batched_blackjax_convergence ready for slow test runs.
  BlackJAX log-posterior and gradient smoke tests validate the pure JAX path.
  _samples_to_idata smoke test validates ArviZ conversion with correct dims.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update SLURM scripts for BlackJAX + multi-GPU</name>
  <files>
    cluster/16_smoke_test_gpu.slurm
    cluster/08_power_sweep.slurm
  </files>
  <action>
1. **Update cluster/16_smoke_test_gpu.slurm:**
   - Update the header comment block to reference Phase 17 / BlackJAX:
     Change "Phase 16 -- GPU Smoke Test (JIT compilation diagnostics)" to
     "Phase 16/17 -- GPU Smoke Test (BlackJAX NUTS + JIT diagnostics)".
   - Update the description to mention BlackJAX NUTS compilation timing
     in addition to CUDA/PTX checks.
   - The actual test invocation (`python scripts/08_run_power_iteration.py
     --smoke-test`) stays UNCHANGED -- it calls fit_batch_hierarchical which
     now defaults to BlackJAX.
   - Add a comment in the "Verify GPU" section noting that BlackJAX supports
     pmap for multi-GPU but this smoke test uses 1 GPU.
   - **Verify smoke test thresholds:** Review the existing --smoke-test gate
     thresholds (cold JIT < 600s, cache speedup > 3x, warm JIT < 120s).
     BlackJAX should be faster than NumPyro, so these thresholds should be
     appropriate or even conservative.  Add a comment noting these thresholds
     were originally set for NumPyro and may be tightened after BlackJAX
     benchmarking confirms typical timings.

2. **Update cluster/08_power_sweep.slurm:**
   - Read the current file first.
   - If `--gres=gpu:1` is present, consider whether to change to `gpu:4` for
     pmap (4 chains across 4 GPUs).  Decision: Keep `gpu:1` as default for
     now -- the vmap fallback works on single GPU and avoids GPU quota issues.
     Add a comment explaining that `gpu:4` can be used for pmap chain
     parallelism if GPU quota allows, with a note that BlackJAX pmap is safe
     on L40S (no psum operations).
   - Update any documentation comments referencing NumPyro to mention BlackJAX
     as the default sampler.

3. **Do NOT modify the Python smoke test script** (scripts/08_run_power_iteration.py)
   -- it calls fit_batch_hierarchical which auto-routes to BlackJAX.
  </action>
  <verify>
  Run `bash -n cluster/16_smoke_test_gpu.slurm` to verify no bash syntax errors.

  Run `bash -n cluster/08_power_sweep.slurm` to verify no bash syntax errors.

  Grep for "BlackJAX" in both files to verify documentation updates.
  </verify>
  <done>
  SLURM smoke test script updated to reference BlackJAX.  Power sweep script
  has pmap documentation comment.  Both scripts pass bash syntax check.
  Smoke test will exercise BlackJAX path via fit_batch_hierarchical default.
  Smoke test thresholds reviewed and annotated for future BlackJAX-specific tuning.
  </done>
</task>

</tasks>

<verification>
1. `pytest tests/test_hierarchical_logp.py -v -k "not slow" --timeout=120` -- all fast tests pass (VALID-01 unchanged, new BlackJAX smoke tests pass).

2. `ruff check tests/test_hierarchical_logp.py` -- lint clean.

3. `bash -n cluster/16_smoke_test_gpu.slurm && bash -n cluster/08_power_sweep.slurm` -- no bash syntax errors.

4. Verify BlackJAX is referenced in SLURM docs: `grep -l "BlackJAX" cluster/*.slurm` returns both files.

5. Verify numpyro fallback test explicitly passes sampler="numpyro": `grep 'sampler="numpyro"' tests/test_hierarchical_logp.py`.
</verification>

<success_criteria>
- All fast tests pass (pytest -k "not slow")
- New BlackJAX smoke tests: log-posterior returns finite scalar, gradient is finite, _samples_to_idata produces correct InferenceData
- VALID-02 BlackJAX test ready (marked slow, calls fit_batch_hierarchical with default sampler)
- VALID-02 numpyro test preserved with explicit sampler="numpyro"
- VALID-03 explicitly deferred to Phase 14 (not tested in this phase)
- Existing VALID-01 and Layer 2 clamping tests unchanged and passing
- SLURM scripts updated with BlackJAX documentation and threshold review notes
- All files pass ruff lint and bash syntax checks
</success_criteria>

<output>
After completion, create `.planning/phases/17-blackjax-nuts-sampler/17-02-SUMMARY.md`
</output>
