---
phase: 14-integration-gpu-benchmark
plan: "14.1-05"
type: execute
wave: 4
depends_on: ["14.1-03"]
files_modified:
  - .planning/phases/14-integration-gpu-benchmark/14.1-05-SUMMARY.md
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "cluster/08_power_sweep.slurm is submitted twice consecutively on M3 with BENCHMARK=1 and the same JAX_COMPILATION_CACHE_DIR target (already set by the SLURM script at line 124 to /scratch/${PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME})"
    - "The JIT timing from the first job (cold cache) is compared to the JIT timing from the second job (warm cache); the second job shows JIT compile time < 5 s versus ~60 s cold, confirming disk-backed JAX compilation cache persistence across separate SLURM job submissions (BENCH-05 cross-process verification)"
    - "Both jobs target the same SLURMD_NODENAME so the per-node cache directory matches; if SLURM schedules them on different nodes the warm-cache test is inconclusive and must be re-run with an explicit --nodelist or the cache path must be refactored node-agnostically"
    - "JIT timings from both job logs are extracted and recorded in the SUMMARY so Phase 14 VERIFICATION.md human-needed #2 can be closed with evidence"
  artifacts:
    - path: ".planning/phases/14-integration-gpu-benchmark/14.1-05-SUMMARY.md"
      provides: "Cross-chunk JIT cache persistence verdict + raw timings"
      contains: "jit_cold_s"
  key_links:
    - from: "cluster/08_power_sweep.slurm submission 1"
      to: "cluster/08_power_sweep.slurm submission 2"
      via: "shared JAX_COMPILATION_CACHE_DIR on the same SLURMD_NODENAME"
      pattern: "JAX_COMPILATION_CACHE_DIR"
---

<objective>
Close VERIFICATION.md human-needed #2: cross-process / cross-SLURM-submission JIT cache persistence. Plan 14.1-03 already measured in-process jit_cold_s vs jit_warm_s in one benchmark run (proving in-memory caching works). BENCH-05's real guarantee — that the SECOND SLURM job started from a cold process on the same node reuses the on-disk JAX_COMPILATION_CACHE_DIR from the first — requires two separate sbatch submissions and a log comparison.

This is the cheapest gap to close: cluster/08_power_sweep.slurm already sets JAX_COMPILATION_CACHE_DIR per-node (line 124) and already echoes it. Submitting BENCHMARK=1 twice and parsing the "Step 2: Cold JIT" / "Step 3: Warm JIT" stdout from _run_benchmark (scripts/08_run_power_iteration.py prints `"  Cold JIT: <val>s"` at ~line 693 and `"  Warm JIT: <val>s"` at ~line 738) gives the two numbers we need.

Depends on 14.1-03 only because we want the BlackJAX-era cold-vs-warm numbers (Phase 17 production sampler), not the numpyro-era ones. If 14.1-03 ran a BlackJAX benchmark on a particular node, the first submission here can re-use that same node to get the warm-cache number directly; otherwise submit two fresh jobs.

Purpose: Gap closure for VERIFICATION.md human-needed #2 (cross-chunk JIT cache persistence).

Output: SUMMARY with jit_cold_s and jit_warm_s from two separate SLURM submissions, and a PASS/FAIL verdict on the < 5 s warm threshold.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
@cluster/08_power_sweep.slurm
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Submit two sequential BENCHMARK=1 runs on the same node</name>
  <what-built>
  cluster/08_power_sweep.slurm already has BENCHMARK=1 mode (line 183) that sets --benchmark and runs _run_benchmark in scripts/08_run_power_iteration.py. JAX_COMPILATION_CACHE_DIR is set to /scratch/${PROJECT}/${USER}/.jax_cache_gpu/${SLURMD_NODENAME} (node-specific, per-process persistent).
  </what-built>
  <how-to-verify>
  On M3:
    1. `cd ~/psilocybin_prl_analyses && git pull origin main`
    2. Submit the FIRST benchmark (cold cache on whichever node SLURM picks). This will compile from scratch:
       - `sbatch --export=ALL,BENCHMARK=1 cluster/08_power_sweep.slurm`
       - Record JOB1_ID (the value SLURM returns; becomes the ARRAY_JOB_ID) and note that cluster/08_power_sweep.slurm uses `%A_%a` for logs, so the task-0 log file is `cluster/logs/power_<JOB1_ID>_0.out`.
    3. Wait for it to complete. `sacct -j <JOB1_ID> --format=JobID,State,NodeList,Elapsed` — record NODE1 (from NodeList column).
    4. Inspect the log: `grep -E "Cold JIT:|Warm JIT:" cluster/logs/power_<JOB1_ID>_0.out` — record the "Cold JIT: <val>s" value reported by _run_benchmark (Step 2 of the benchmark); the Warm JIT value on the same job is in-process and not what we want — the cross-submission warm timing comes from JOB2 below.
    5. Submit the SECOND benchmark, CONSTRAINED to the same node so the per-node JAX cache matches:
       - `sbatch --export=ALL,BENCHMARK=1 --nodelist=<NODE1> cluster/08_power_sweep.slurm`
       - Record JOB2_ID.
    6. Wait for completion. `sacct -j <JOB2_ID> --format=JobID,State,NodeList,Elapsed` — confirm NodeList == NODE1.
    7. Inspect the second log: `grep -E "Cold JIT:|Warm JIT:" cluster/logs/power_<JOB2_ID>_0.out` — record the "Cold JIT: <val>s" value from JOB2. This is the cross-submission warm-cache number (JOB2's "Cold JIT" is the first compile in JOB2's fresh process, but reads the on-disk cache written by JOB1; expect < 5 s if the JAX_COMPILATION_CACHE_DIR persisted).

  Expected: first JIT ~60 s, second JIT < 5 s.

  Note on --array=0-2 default in cluster/08_power_sweep.slurm: both jobs will spawn 3 array tasks (chunks 0, 1, 2). Task 0 is enough to read the JIT timing from; tasks 1-2 will re-use the same cache in-memory. You can reduce to a single task with `sbatch --array=0 ...` to save cluster hours if desired.

  If NODE1 is unavailable at submission time (maintenance, queue), the plan can be re-run on any node — just ensure both submissions land on the SAME NODE and record that fact in the SUMMARY.
  </how-to-verify>
  <resume-signal>
  Paste:
    - "JOB1_ID=<id> on NODE1=<name>, JIT cold time = <value>s"
    - "JOB2_ID=<id> on NODE1 (same node), JIT warm time = <value>s"
    - OR "nodelist mismatch: JOB1 on <nodeA>, JOB2 on <nodeB> — results inconclusive, need to resubmit"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record verdict in SUMMARY</name>
  <files>.planning/phases/14-integration-gpu-benchmark/14.1-05-SUMMARY.md</files>
  <action>
  Using the two JIT timings from Task 1, write the SUMMARY with:
    - jit_cold_s (from JOB1)
    - jit_warm_s (from JOB2 on the same node)
    - Verdict: PASSED if jit_warm_s < 5.0 else FAILED
    - Node used for both runs
    - Sampler (blackjax, assumed based on 14.1-03 default)
    - Comment on what the numbers imply for Phase 15 — if PASSED, Phase 15 can rely on warm-cache JIT across array-task boundaries (~ seconds of JIT overhead per chunk instead of ~ 1 min, savings of ~3 min across 3 chunks); if FAILED, the SLURM script's JAX_COMPILATION_CACHE_DIR setup at line 124 needs debugging (most likely cause: /scratch path not readable or the cache files don't survive between jobs due to scratch cleanup policy).

  Commit:
    git add .planning/phases/14-integration-gpu-benchmark/14.1-05-SUMMARY.md
    git commit -m "docs(14.1-05): BENCH-05 cross-submission JIT cache verdict"
    git push origin main
  </action>
  <verify>
  1. SUMMARY.md exists with jit_cold_s, jit_warm_s, verdict, node name, and commit pushed.
  2. If PASSED: jit_warm_s < 5.0.
  3. If FAILED: SUMMARY identifies which check failed (cache dir missing, files absent, or warm JIT not improved).
  </verify>
  <done>
  VERIFICATION.md human-needed #2 has a written verdict; Phase 14 re-verification (plan 14.1-06) can read it.
  </done>
</task>

</tasks>

<verification>
1. Two SLURM submissions completed on the same node with BENCHMARK=1.
2. JIT cold time and warm time extracted from logs.
3. SUMMARY records verdict (< 5 s warm = PASSED).
</verification>

<success_criteria>
- VERIFICATION.md human-needed #2 (cross-chunk JIT cache) resolved with evidence.
- SUMMARY readable by Phase 14 re-verification.
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-05-SUMMARY.md` per Task 2.
</output>
