---
phase: 14-integration-gpu-benchmark
plan: "14.1-04"
type: execute
wave: 3
depends_on: []
files_modified:
  - results/power/valid03_cpu.json
  - results/power/valid03_gpu.json
  - cluster/14_valid03_cpu.slurm
  - cluster/14_valid03_gpu.slurm
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "cluster/14_valid03_cpu.slurm and cluster/14_valid03_gpu.slurm have git auto-push blocks added (mirroring 18_smoke_patrl_cpu.slurm) so their valid03_{cpu,gpu}.json outputs land on origin/main automatically"
    - "Both SLURM jobs submit and complete; results/power/valid03_cpu.json and results/power/valid03_gpu.json exist on the local machine after git pull"
    - "validation/valid03_cross_platform.py compare results/power/valid03_cpu.json results/power/valid03_gpu.json --rtol 0.01 returns exit 0 (True) — all per-participant posterior-mean relative errors below 1% for every parameter"
    - "Compare output (relative errors per parameter) is recorded in the 14.1-04-SUMMARY.md so future verifiers can see the numbers without re-running the job"
  artifacts:
    - path: "results/power/valid03_cpu.json"
      provides: "VALID-03 CPU baseline posterior means"
      contains: "per_participant"
    - path: "results/power/valid03_gpu.json"
      provides: "VALID-03 GPU posterior means"
      contains: "per_participant"
    - path: "cluster/14_valid03_cpu.slurm"
      provides: "CPU valid03 with git auto-push block"
      contains: "git push origin main"
    - path: "cluster/14_valid03_gpu.slurm"
      provides: "GPU valid03 with git auto-push block"
      contains: "git push origin main"
  key_links:
    - from: "results/power/valid03_cpu.json"
      to: "results/power/valid03_gpu.json"
      via: "validation/valid03_cross_platform.py compare"
      pattern: "compare"
    - from: "cluster/14_valid03_{cpu,gpu}.slurm"
      to: "origin/main"
      via: "git auto-push block"
      pattern: "git push origin main"
---

<objective>
Close the VALID-03 gap from Phase 14 VERIFICATION.md. Two SLURM submissions + one compare step need to run on M3: a CPU fit (partition=comp) and a GPU fit (partition=gpu) of the same small batched hierarchical model, then a compare step that asserts posterior means agree within 1% relative error.

This plan also patches cluster/14_valid03_{cpu,gpu}.slurm with git auto-push blocks so the JSONs land on the local machine without manual scp. cluster/14_valid03_compare.slurm already reads both JSONs from results/power/ and exits non-zero if the tolerance is violated — so it can stay as-is or be run locally after git pull; this plan runs compare locally for speed.

Purpose: Gap closure for VERIFICATION.md SC5 (VALID-03 CPU vs GPU 1% relative error never verified).

Output: valid03_cpu.json + valid03_gpu.json on origin/main, compare_results returns True, numerical relative errors per parameter captured in SUMMARY.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
@cluster/14_valid03_cpu.slurm
@cluster/14_valid03_gpu.slurm
@cluster/14_valid03_compare.slurm
@cluster/18_smoke_patrl_cpu.slurm
@validation/valid03_cross_platform.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add git auto-push blocks to valid03 CPU and GPU SLURM scripts</name>
  <files>cluster/14_valid03_cpu.slurm, cluster/14_valid03_gpu.slurm</files>
  <action>
  Patch both cluster/14_valid03_cpu.slurm and cluster/14_valid03_gpu.slurm with a git auto-push block mirroring cluster/18_smoke_patrl_cpu.slurm lines 133-156.

  For cluster/14_valid03_cpu.slurm:
  Before the final `exit $EXIT_CODE` (line 68), insert:

  ```bash
  # =============================================================================
  # Auto-push logs + valid03 JSON to git (mirrors 18_smoke_patrl_cpu.slurm)
  # =============================================================================
  echo ""
  echo "Pushing valid03 CPU results to git..."
  _LOG_OUT="cluster/logs/valid03_cpu_${SLURM_JOB_ID}.out"
  _LOG_ERR="cluster/logs/valid03_cpu_${SLURM_JOB_ID}.err"
  _VALID_JSON="results/power/valid03_cpu.json"

  git add "${_LOG_OUT}" "${_LOG_ERR}" 2>/dev/null
  git add "${_VALID_JSON}" 2>/dev/null
  if ! git diff --cached --quiet 2>/dev/null; then
      git commit -m "valid03 cpu (job ${SLURM_JOB_ID})"
      if git push origin main 2>/dev/null; then
          echo "Pushed valid03 CPU results to origin/main"
      else
          echo "WARNING: git push failed — results committed locally only"
      fi
  else
      echo "No new files to push"
  fi
  ```

  For cluster/14_valid03_gpu.slurm:
  Same pattern, but _LOG_OUT/_LOG_ERR/_VALID_JSON variables use the gpu suffix (valid03_gpu_<id>.out/.err and valid03_gpu.json). Commit message: `valid03 gpu (job ${SLURM_JOB_ID})`.

  Run regardless of EXIT_CODE so .err logs upload on failure too.

  Do NOT add an auto-push block to cluster/14_valid03_compare.slurm — its only output is stdout and it runs on comp partition quickly; Task 3 runs compare locally instead.
  </action>
  <verify>
  1. `grep -c "git push origin main" cluster/14_valid03_cpu.slurm` returns 1.
  2. `grep -c "git push origin main" cluster/14_valid03_gpu.slurm` returns 1.
  3. `grep "_VALID_JSON" cluster/14_valid03_cpu.slurm` shows the variable set to results/power/valid03_cpu.json.
  4. `grep "_VALID_JSON" cluster/14_valid03_gpu.slurm` shows the variable set to results/power/valid03_gpu.json.
  5. `bash -n cluster/14_valid03_cpu.slurm` and `bash -n cluster/14_valid03_gpu.slurm` both exit 0.
  </verify>
  <done>
  Both valid03 SLURM scripts have git auto-push blocks; syntax valid; commit messages tagged with job ID and platform.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit valid03 CPU and GPU SLURM jobs on M3</name>
  <what-built>
  cluster/14_valid03_cpu.slurm and cluster/14_valid03_gpu.slurm, now with auto-push blocks from Task 1 landed on origin/main.
  </what-built>
  <how-to-verify>
  On M3:
    1. `cd ~/psilocybin_prl_analyses`
    2. `git pull origin main` — confirm the Task 1 patches arrived.
    3. `grep "git push origin main" cluster/14_valid03_cpu.slurm cluster/14_valid03_gpu.slurm` — shows both have the push block.
    4. Submit both jobs. They are INDEPENDENT — submit in parallel:
       - `sbatch cluster/14_valid03_cpu.slurm` — records CPU_JOB_ID
       - `sbatch cluster/14_valid03_gpu.slurm` — records GPU_JOB_ID
    5. Monitor: `squeue -u $USER -n prl_v03cpu,prl_v03gpu`
    6. When both finish: confirm they COMPLETED via `sacct -j <CPU_JOB_ID>,<GPU_JOB_ID> --format=JobID,State,ExitCode,Elapsed`
    7. The auto-push blocks push their JSONs + logs. Confirm on cluster: `git log origin/main --oneline -5` shows `valid03 cpu (job <CPU_JOB_ID>)` AND `valid03 gpu (job <GPU_JOB_ID>)`.

  On local:
    8. `git pull origin main`
    9. `ls results/power/valid03_cpu.json results/power/valid03_gpu.json` — both exist.

  Expected: --time=04:00:00 on cpu, --time=08:00:00 on gpu; with --n-per-group 3 --n-chains 2 --n-draws 200 --n-tune 200 this is a SMALL fit and should complete in much less than walltime on both platforms.
  </how-to-verify>
  <resume-signal>
  Paste:
    - "both completed; CPU_JOB_ID=<id>, GPU_JOB_ID=<id>; JSONs visible locally after git pull"
    - OR "cpu failed: <err paste>" / "gpu failed: <err paste>" — Claude will diagnose
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run compare locally and record per-parameter relative errors</name>
  <files>.planning/phases/14-integration-gpu-benchmark/14.1-04-SUMMARY.md</files>
  <action>
  Assumes Task 2 resolved with both JSONs present locally.

  Step 1 — Run compare locally (no SLURM needed; the compare is a pure Python call with no GPU/CPU dependency):

    python validation/valid03_cross_platform.py compare \
        results/power/valid03_cpu.json \
        results/power/valid03_gpu.json \
        --rtol 0.01

  Capture stdout AND the exit code. Exit 0 means all per-participant posterior-mean relative errors for every parameter are below 1%.

  Step 2 — If exit 0: write SUMMARY with the max relative error per parameter (parsed from compare stdout or computed manually from the JSONs), the threshold (1%), the n_per_group (3) and chain/draw settings, and the VALID-03 verdict: PASSED.

  Step 3 — If exit != 0: the compare script prints which parameter(s) exceeded 1% and by how much. Surface that information verbatim in the SUMMARY and STOP. Do not modify tolerance to force a pass — VALID-03 with rtol=0.01 is a locked requirement. If it fails, the failure mode becomes a new investigation (likely a JAX numerics issue between x64 on CPU vs GPU, or a PyMC/numpyro-direct sampler seed mismatch).

  Step 4 — Commit SUMMARY:
    git add .planning/phases/14-integration-gpu-benchmark/14.1-04-SUMMARY.md
    git commit -m "docs(14.1-04): VALID-03 cross-platform verdict"
    git push origin main

  Note: cluster/14_valid03_compare.slurm is a useful backup if the user prefers to run the compare on the cluster, but it's faster to run locally since the compare step is just JSON-diffing.
  </action>
  <verify>
  1. SUMMARY.md exists and contains: per-parameter max relative error, verdict (PASSED or FAILED), rtol=0.01 threshold, input file paths.
  2. If PASSED: `python validation/valid03_cross_platform.py compare results/power/valid03_cpu.json results/power/valid03_gpu.json --rtol 0.01` exits 0.
  3. If FAILED: SUMMARY documents which parameter exceeded tolerance and by how much; plan does NOT force a pass via tolerance relaxation.
  </verify>
  <done>
  VALID-03 verdict recorded in SUMMARY; if PASSED, Phase 14 SC5 is closed; if FAILED, a new investigation is triaged but gap closure for 14.1-04 is complete — the point of VALID-03 is to PRODUCE the verdict, not to guarantee success.
  </done>
</task>

</tasks>

<verification>
1. Phase 14 VERIFICATION.md SC5 (VALID-03 CPU vs GPU 1% rel err) — addressed: verdict is produced, pass or fail.
2. Both valid03 SLURM scripts have git auto-push blocks (consistent with the quick-005 convention).
3. results/power/valid03_{cpu,gpu}.json on origin/main.
4. 14.1-04-SUMMARY.md records the per-parameter max relative error and verdict.
</verification>

<success_criteria>
- Both valid03 JSONs exist on origin/main after cluster runs.
- Compare step run locally with rtol=0.01.
- Verdict recorded in SUMMARY; if FAILED, root-cause investigation surfaced (does not gate this plan's completion).
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-04-SUMMARY.md` with:
- VALID-03 verdict (PASSED or FAILED)
- Max relative error per parameter (omega_2, omega_3, kappa, beta, zeta, mu_3_0)
- Threshold used (0.01 = 1%)
- n_per_group, chains, draws, tune
- CPU and GPU SLURM job IDs
- If FAILED: which parameters exceeded, by how much, and recommended next step
</output>
