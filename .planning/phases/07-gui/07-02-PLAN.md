---
phase: 07-gui
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - notebooks/07_parameter_explorer.ipynb
autonomous: false

must_haves:
  truths:
    - "Opening the notebook in VSCode shows sliders and a multi-panel plot"
    - "Moving any slider updates the plot within <2 seconds"
    - "Toggling model type shows/hides the volatility trajectory panel"
    - "Pre-set profiles load instantly and produce visibly different patterns"
    - "Ground-truth reward probabilities are overlaid on belief trajectories"
  artifacts:
    - path: "notebooks/07_parameter_explorer.ipynb"
      provides: "Interactive GUI notebook for parameter exploration"
      contains: "ParamExplorer"
  key_links:
    - from: "notebooks/07_parameter_explorer.ipynb"
      to: "prl_hgf.gui"
      via: "from prl_hgf.gui import ParamExplorer"
      pattern: "from prl_hgf\\.gui import ParamExplorer"
---

<objective>
Create the interactive Jupyter notebook that serves as the user-facing GUI for
parameter exploration, then verify the full interactive experience.

Purpose: The notebook is the final deliverable — users open it in VSCode and
immediately get an interactive dashboard for exploring HGF parameter effects.

Output: A working `notebooks/07_parameter_explorer.ipynb` verified by human
interaction.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gui/07-RESEARCH.md
@.planning/phases/07-gui/07-01-SUMMARY.md
@src/prl_hgf/gui/explorer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create parameter explorer notebook</name>
  <files>notebooks/07_parameter_explorer.ipynb</files>
  <action>
Create `notebooks/` directory if it does not exist, then create
`notebooks/07_parameter_explorer.ipynb` as a Jupyter notebook (.ipynb JSON).

The notebook should be a thin wrapper around ParamExplorer. Structure:

**Cell 1 (markdown):**
```
# PRL HGF Parameter Explorer

Interactive dashboard for exploring how HGF parameters affect belief
trajectories, learning rates, and choice probabilities on the PRL
pick_best_cue task.

**Usage:** Run all cells. The first cell warms up JAX (~5-10 seconds on first
run). After that, slider changes update the plot within ~2 seconds.

**Panels:**
1. **Belief trajectories** — P(reward|cue) with ground-truth overlay (dashed)
2. **Volatility** — Shared volatility estimate mu2 (3-level model only)
3. **Choice probabilities** — P(choose cue) from softmax + stickiness
4. **Effective learning rate** — How much each cue's belief updates per trial
```

**Cell 2 (code):**
```python
%matplotlib widget
```

**Cell 3 (code):**
```python
from prl_hgf.gui import ParamExplorer

explorer = ParamExplorer()
```

**Cell 4 (code):**
```python
explorer.display()
```

**Cell 5 (markdown):**
```
## Parameter Guide

| Parameter | Symbol | Effect |
|-----------|--------|--------|
| omega_2   | omega2  | Tonic volatility — higher = faster learning, more noise-sensitive |
| omega_3   | omega3  | Meta-volatility — how quickly volatility estimate changes (3-level only) |
| kappa     | kappa   | Coupling strength — how much volatility drives learning rate (3-level only) |
| beta      | beta    | Inverse temperature — higher = more exploitative, lower = more random |
| zeta      | zeta    | Stickiness — positive = perseverate on previous choice |
| mu_1_0    | mu1(0)  | Initial belief about P(reward) for all cues |
| mu_3_0    | mu3(0)  | Initial volatility prior mean (3-level only) |

## Preset Profiles

- **healthy baseline:** omega2=-3.0, kappa=1.0, beta=2.5 — balanced learning
- **post-concussion:** omega2=-4.0, kappa=0.8, beta=2.0 — slower learning, more perseveration
- **post-psilocybin:** omega2=-2.5, kappa=1.1, beta=2.0 — faster learning, increased coupling

Note: Belief trajectories are computed on a single simulated session (seed=0)
with fixed choices/rewards. The forward pass uses batch input_data (not
trial-by-trial simulation) for interactive speed.
```

Write the notebook as valid .ipynb JSON with proper cell structure, metadata,
and kernel specification (python3).
  </action>
  <verify>
Verify the notebook file is valid JSON: `conda run -n ds_env python -c "import json; json.load(open('notebooks/07_parameter_explorer.ipynb'))"`.
Verify the import works: `conda run -n ds_env python -c "from prl_hgf.gui import ParamExplorer; print('OK')"`.
  </verify>
  <done>
notebooks/07_parameter_explorer.ipynb exists as valid .ipynb JSON with 5 cells
(2 markdown, 3 code), imports ParamExplorer, and uses %matplotlib widget backend.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Interactive parameter exploration GUI notebook with:
- 7 parameter sliders (omega_2, omega_3, kappa, beta, zeta, mu_1_0, mu_3_0)
- 4-panel plot (beliefs, volatility, choice probs, learning rate)
- Model toggle (2-level / 3-level)
- Preset profiles (healthy baseline, post-concussion, post-psilocybin)
- Ground-truth reward probability overlay (dashed lines)
  </what-built>
  <how-to-verify>
1. Open `notebooks/07_parameter_explorer.ipynb` in VSCode
2. Select the `ds_env` kernel
3. Run all cells — first cell should show "Warming up JAX..." message and take ~5-10s
4. Verify the dashboard appears with sliders on the left and a 4-panel plot
5. Move the omega_2 slider — plot should update within ~2 seconds
6. Move the beta slider — plot should update near-instantly (no forward pass)
7. Click "2-level" toggle — volatility panel should disappear, omega_3/kappa/mu_3_0 sliders should hide
8. Click "3-level" toggle — volatility panel and sliders should reappear
9. Click "post-concussion" preset — all sliders should snap to preset values, plot updates
10. Click "post-psilocybin" preset — visibly different belief trajectories (faster learning)
11. Verify ground-truth reward probabilities appear as dashed lines overlaid on belief trajectories
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues with the interactive experience</resume-signal>
</task>

</tasks>

<verification>
1. Notebook opens in VSCode and runs without errors
2. All 5 success criteria from the roadmap are met:
   - Sliders visible for all parameters
   - Slider updates plot within <2 seconds
   - Model toggle shows/hides volatility panel
   - Presets load instantly with visibly different patterns
   - Ground-truth overlay present
</verification>

<success_criteria>
- Notebook opens in VSCode with ds_env kernel and runs all cells
- Dashboard shows sliders + 4-panel plot
- Slider changes update plot within <2 seconds
- Model toggle works (show/hide volatility)
- Preset profiles produce visibly different belief patterns
- Ground-truth dashed lines overlay on belief trajectories
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/07-gui/07-02-SUMMARY.md`
</output>
