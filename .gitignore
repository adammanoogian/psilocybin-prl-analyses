# Generated artifacts (transient logs ignored by default; phase25 cluster
# outputs, SLURM-run logs auto-pushed by cluster/*.slurm, and .gitkeep
# markers are tracked)
logs/*
!logs/.gitkeep
!logs/phase25/
!logs/patrl18smoke_*
!logs/bench14_*
!logs/bench14cpu_*
!logs/smoke16_*
!logs/smoke16cpu_*
# Whitelist all SLURM .err/.out at every depth so cluster autopush
# surfaces job failures locally regardless of whether the SLURM template
# writes flat (logs/<job>_%j.err) or nested (logs/phase25/recovery_%j.err).
# Otherwise we work blind and have to hand-push error logs from M3.
!logs/*.err
!logs/*.out
!logs/**/
!logs/**/*.err
!logs/**/*.out

# CCDS canonical (Phase 26 Wave 3): models/ contents gitignored (heavy
# artifacts); tracked via .gitkeep markers. reports/ is tracked (small
# CSV/MD outputs are paper-ready and version-worthy). Cluster auto-push
# subtrees (patrl_smoke/, power/) are whitelisted so SLURM auto-push
# commits land. Uses models/* + !subdir pattern (NOT bare models/) so
# subdirectory whitelists work — bare models/ with trailing slash blocks
# any subdir re-include per git-ignore semantics.
models/*
results/*
!models/.gitkeep
!models/mle/.gitkeep
!models/bayesian/.gitkeep
!models/patrl_smoke/
!models/power/

# GSD planning audit trail — local-only, not pushed (per user policy)
.planning/

# Data (not committed - too large / sensitive)
data/*
# ...but viz_fixtures/ ships schema-round-trip inputs (Phase 22+) -- commit them
!data/viz_fixtures/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/
.installed.cfg
lib/
lib64/
parts/
sdist/
var/
wheels/
MANIFEST
.Python
pip-wheel-metadata/

# Virtual environments
.venv/
/venv/
/ENV/
/env/
.env

# Jupyter
.ipynb_checkpoints
*.ipynb_checkpoints/
profile_default/
ipython_config.py

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Testing and coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
*.cover
*.py,cover
coverage.xml
nosetests.xml
test-results/

# MyPy
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff
.ruff_cache/

# JAX compilation cache
jax_cache/

# Phase 25: cloned ELK reference implementation (do not commit into hgf-analysis)
.planning/phases/25-parallel-scan-acceleration-research/scratch/elk/

# Claude Code harness state (per-machine lockfiles, not source)
.claude/

# Temporary
*.tmp
*.bak
*.orig

# OS
Thumbs.db
bash.exe.stackdump

# Slurm job outputs
slurm-*.out
smux-*.out
smux-*.err
workflow/.snakemake/
*.stackdump
