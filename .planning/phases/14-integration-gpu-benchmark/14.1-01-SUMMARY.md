---
phase: 14-integration-gpu-benchmark
plan: "14.1-01"
subsystem: infra
tags: [slurm, blackjax, numpyro, git-autopush, benchmark]

# Dependency graph
requires:
  - phase: quick-005
    provides: git auto-push pattern established in 18_smoke_patrl_cpu.slurm
  - phase: 17-blackjax-sampler
    provides: BlackJAX as default production sampler (decision 98)
provides:
  - SAMPLER env-var parameterization for 14_benchmark_gpu.slurm (default blackjax)
  - git auto-push block pushing bench logs + benchmark_batched.json to origin/main
affects:
  - Phase 14 gap closure (remaining 14.1-0x plans)
  - Cluster benchmark re-submissions using BlackJAX

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SLURM env-var parameterization: export VAR=\"${VAR:-default}\" near invocation"
    - "git auto-push block mirrors quick-005 pattern: add logs + results + STATE.md, commit if staged diff non-empty, push with warning fallback"

key-files:
  created: []
  modified:
    - cluster/14_benchmark_gpu.slurm

key-decisions:
  - "SAMPLER defaults to blackjax matching Phase 17 production sampler decision (STATE decision 98)"
  - "Auto-push runs regardless of EXIT_CODE so failed bench still uploads its .err log"
  - "Pure file edit — running numpyro benchmark job not interrupted"

patterns-established:
  - "SAMPLER env-var: default-blackjax pattern usable for future benchmark SLURM scripts"

# Metrics
duration: 5min
completed: 2026-04-18
---

# Phase 14.1 Plan 01: SAMPLER env-var + git auto-push for 14_benchmark_gpu.slurm Summary

**14_benchmark_gpu.slurm patched with SAMPLER env-var (default blackjax) and a git auto-push block that stages bench logs + benchmark_batched.json and pushes to origin/main after every run**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-18T14:56:16Z
- **Completed:** 2026-04-18T14:58:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- `export SAMPLER="${SAMPLER:-blackjax}"` added before python invocation; `--sampler numpyro` hard-code replaced with `--sampler "${SAMPLER}"`
- Usage docstring updated with both invocation forms (blackjax default, numpyro legacy via `--export=ALL,SAMPLER=numpyro`)
- Sampler echoed in the header block alongside Job ID / Node / GPU
- Git auto-push block added (mirrors `18_smoke_patrl_cpu.slurm` lines 133-156): stages `.out`/`.err` logs, `results/power/benchmark_batched.json`, `.planning/STATE.md`; commits with `bench phase14 gpu sampler=${SAMPLER} (job ${SLURM_JOB_ID})`; pushes to origin/main with warning fallback; block runs regardless of EXIT_CODE

## Task Commits

1. **Task 1: Parameterize sampler via SAMPLER env var (default blackjax)** - `8a017d6` (fix)
2. **Task 2: Add git auto-push block mirroring 18_smoke_patrl_cpu.slurm** - `c399000` (fix)

**Plan metadata:** (docs commit below)

## Files Created/Modified

- `cluster/14_benchmark_gpu.slurm` - SAMPLER env-var, updated usage docstring, echo in header, and git auto-push block

## Decisions Made

- SAMPLER defaults to `blackjax` — aligns with STATE decision 98 (Phase 17): BlackJAX is default production sampler
- Auto-push block runs unconditionally before `exit $EXIT_CODE` so even a failed benchmark uploads its `.err` log
- No behavior change to already-running numpyro benchmark job — pure file edit, no job interruption

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- `cluster/14_benchmark_gpu.slurm` ready for re-submission with default BlackJAX sampler
- Re-submit with `sbatch cluster/14_benchmark_gpu.slurm` to get Phase 17 production benchmark data
- Legacy numpyro comparison: `sbatch --export=ALL,SAMPLER=numpyro cluster/14_benchmark_gpu.slurm`
- No blockers for remaining 14.1 gap-closure plans

---
*Phase: 14-integration-gpu-benchmark*
*Completed: 2026-04-18*
