---
phase: 14-integration-gpu-benchmark
plan: "14.1-03"
type: execute
wave: 3
depends_on: ["14.1-01", "14.1-02"]
files_modified:
  - results/power/benchmark_batched.json
  - .planning/STATE.md
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "cluster/14_benchmark_gpu.slurm is re-submitted on M3 with SAMPLER=blackjax (or no override; blackjax is the default after plan 14.1-01)"
    - "The re-submission writes a BlackJAX-sampler Phase 14 benchmark JSON to results/power/benchmark_batched.json on the cluster, and the script's git auto-push block (added in 14.1-01) uploads it to origin/main automatically"
    - "STATE.md Key Decisions table gains a second row labelled sampler=blackjax with the BlackJAX run's per_iter_s, gpu_hours_per_chunk, and decision — this is the row Phase 15 will consume when picking partition/walltime"
    - "The BlackJAX benchmark JSON overwrites the numpyro JSON at results/power/benchmark_batched.json; the numpyro version is preserved in git history (via the 14.1-02 commit) and is also archived to results/power/benchmark_batched_numpyro.json before re-running so both are side-by-side on disk"
  artifacts:
    - path: "results/power/benchmark_batched.json"
      provides: "BlackJAX Phase 14 benchmark output (production sampler)"
      contains: "per_iteration_s"
    - path: "results/power/benchmark_batched_numpyro.json"
      provides: "Archived numpyro baseline for side-by-side comparison"
      contains: "per_iteration_s"
    - path: ".planning/STATE.md"
      provides: "Benchmark decision gate row sampler=blackjax"
      contains: "Benchmark decision gate sampler=blackjax"
  key_links:
    - from: "cluster/14_benchmark_gpu.slurm"
      to: "results/power/benchmark_batched.json"
      via: "git auto-push block pushes benchmark result to origin/main"
      pattern: "git push origin main"
    - from: "results/power/benchmark_batched.json (blackjax)"
      to: ".planning/STATE.md sampler=blackjax row"
      via: "_update_state_md appends row labelled sampler=blackjax"
      pattern: "Benchmark decision gate sampler=blackjax"
---

<objective>
Re-submit cluster/14_benchmark_gpu.slurm on M3 with the BlackJAX sampler so Phase 15's production partition/walltime decision is made against the same sampler Phase 15 will use. STATE.md decisions 98 (BlackJAX default) and 105 (JIT gate thresholds are numpyro-conservative and will tighten post-cluster-bench) both require a BlackJAX bench pass.

This plan depends on 14.1-01 (SLURM patch: SAMPLER env var + git auto-push) being merged AND 14.1-02 (numpyro results landed locally, numpyro STATE.md row written, benchmark JSON archived).

Purpose: Gap closure for a post-VERIFICATION concern — Phase 15 production will use BlackJAX, but the only benchmark-JSON-on-disk is currently numpyro. Committing to a Phase 15 partition without a BlackJAX measurement risks either over-provisioning (if BlackJAX is faster) or walltime-failing (if slower).

Output: results/power/benchmark_batched.json replaced with BlackJAX run; numpyro archived alongside; STATE.md gains a second decision-gate row labelled sampler=blackjax.
</objective>

<execution_context>
@C:\Users\aman0087\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\aman0087\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-integration-gpu-benchmark/14-VERIFICATION.md
@.planning/phases/14-integration-gpu-benchmark/14.1-01-PLAN.md
@.planning/phases/14-integration-gpu-benchmark/14.1-02-PLAN.md
@cluster/14_benchmark_gpu.slurm
</context>

<tasks>

<task type="auto">
  <name>Task 1: Archive numpyro benchmark JSON before re-running</name>
  <files>results/power/benchmark_batched_numpyro.json</files>
  <action>
  Rename results/power/benchmark_batched.json (which contains the numpyro baseline from plan 14.1-02) to results/power/benchmark_batched_numpyro.json. Use git mv so history follows:

    git mv results/power/benchmark_batched.json results/power/benchmark_batched_numpyro.json
    git commit -m "archive(14.1): preserve numpyro benchmark before blackjax re-run"
    git push origin main

  Rationale: the Phase 14 benchmark script writes to a fixed filename (results/power/benchmark_batched.json); re-submitting with blackjax will overwrite it. Archiving the numpyro version to a _numpyro suffix keeps both side-by-side on disk for comparison, while the "canonical" benchmark_batched.json becomes the BlackJAX (production sampler) result that Phase 15 consumes.

  Also push before submitting the SLURM job — the cluster needs to git pull this archive operation before the BlackJAX run starts writing to benchmark_batched.json, otherwise the cluster's git auto-push block (added in 14.1-01) will see the old numpyro JSON as "deleted + added" rather than as "a fresh write".
  </action>
  <verify>
  1. `ls results/power/benchmark_batched_numpyro.json` exists.
  2. `ls results/power/benchmark_batched.json` does NOT exist (will be recreated by the BlackJAX SLURM job).
  3. `git log --oneline -1 -- results/power/benchmark_batched_numpyro.json` shows the archive commit.
  4. `git push origin main` succeeded (ref on origin matches local HEAD).
  </verify>
  <done>
  Numpyro benchmark JSON archived under results/power/benchmark_batched_numpyro.json; working tree clean; origin/main advanced.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Submit BlackJAX benchmark on M3</name>
  <what-built>
  cluster/14_benchmark_gpu.slurm patched in 14.1-01 with SAMPLER env var (default blackjax) and git auto-push block. The patch is on origin/main after 14.1-01 committed.
  </what-built>
  <how-to-verify>
  On M3:
    1. `cd ~/psilocybin_prl_analyses` (or wherever the repo lives on M3)
    2. `git pull origin main` — confirms 14.1-01 patch + 14.1-02 numpyro archive are present.
    3. Verify patch landed: `grep -n "SAMPLER" cluster/14_benchmark_gpu.slurm` shows the SAMPLER export and `--sampler "${SAMPLER}"` plumbing. `grep -n "git push origin main" cluster/14_benchmark_gpu.slurm` shows the auto-push block.
    4. Confirm no stale benchmark JSON remains: `ls results/power/benchmark_batched.json 2>&1` should say "No such file or directory".
    5. Submit: `sbatch cluster/14_benchmark_gpu.slurm`
       (no --export needed; blackjax is the default after 14.1-01)
    6. Record the returned JOB_ID.
    7. Monitor: `squeue -u $USER -n prl_bench14` shows it PD then R; `tail -f cluster/logs/bench14_<JOB_ID>.out` once it starts running.
    8. When it finishes: the git auto-push block in the SLURM script (added in 14.1-01) commits + pushes cluster/logs/bench14_<JOB_ID>.{out,err}, results/power/benchmark_batched.json, and .planning/STATE.md to origin/main. Confirm the push arrived: `git log origin/main --oneline -3` on the cluster.

  On local machine:
    9. `git pull origin main` — pulls the BlackJAX benchmark JSON + STATE.md update + logs.
    10. `python -m json.tool results/power/benchmark_batched.json` shows valid JSON with mcmc_settings.sampler == "numpyro-direct" OR sampler field showing "blackjax" (note: the script hard-codes sampler="numpyro-direct" in the mcmc_settings dict at line 1087 — if that wasn't updated to read from args.sampler, that's a minor schema drift to surface but not block on; the per_iteration_s is the load-bearing field).

  Expected outcome: results/power/benchmark_batched.json is a fresh BlackJAX run; STATE.md has a new Benchmark decision gate row; numpyro archive is preserved at results/power/benchmark_batched_numpyro.json.
  </how-to-verify>
  <resume-signal>
  Paste one of:
    - "blackjax benchmark completed, job <JOB_ID>, decision=<gpu|cpu_comp>, gpu_hours_per_chunk=<value>, per_iteration_s=<value>"
    - "blackjax benchmark failed, err log: <paste last 40 lines>"
    - "blackjax benchmark timed out at 8h — numbers to date: <paste>"
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Disambiguate the BlackJAX STATE.md row and write comparison summary</name>
  <files>.planning/STATE.md</files>
  <action>
  Assumes Task 2 resumed with success and `git pull origin main` landed the BlackJAX benchmark JSON + STATE.md update.

  Step 1 — Row label disambiguation:
  The cluster-side _update_state_md appended a row labelled "Benchmark decision gate" (no sampler tag — scripts/08_run_power_iteration.py line 274). But the numpyro row from 14.1-02 was already labelled "Benchmark decision gate sampler=numpyro". Now there are TWO rows. Edit the BlackJAX row to change its first column from
    `Benchmark decision gate: <decision> (<hours> GPU-hrs/chunk)`
  to
    `Benchmark decision gate sampler=blackjax: <decision> (<hours> GPU-hrs/chunk)`
  Change the phase tag column from `14-02` to `14.1-03`.

  Step 2 — Numerical comparison:
  Read both JSONs:
    - results/power/benchmark_batched.json (BlackJAX, current)
    - results/power/benchmark_batched_numpyro.json (NumPyro, archived)
  Compute and print per_iteration_s ratio, gpu_hours_per_chunk ratio, and confirm decisions match (both should reach the same gpu/cpu_comp verdict unless they straddle the 50h threshold).

  Step 3 — Commit:
    git add .planning/STATE.md
    git commit -m "docs(14.1): label blackjax benchmark row + compare to numpyro"
    git push origin main

  Do NOT revise the JIT gate thresholds in apply_decision_gate in this plan — STATE.md decision 105 says thresholds will tighten post-cluster-bench, but that threshold tightening belongs in a future plan (Phase 15 planning or a quick-NNN), not gap closure. Surface a recommendation in the SUMMARY about whether the BlackJAX numbers warrant a threshold tightening, but leave the code alone.
  </action>
  <verify>
  1. `grep -c "Benchmark decision gate" .planning/STATE.md` returns >= 2.
  2. `grep "sampler=numpyro" .planning/STATE.md` returns 1 row; `grep "sampler=blackjax" .planning/STATE.md` returns 1 row.
  3. `grep -cE "Benchmark decision gate:( |$)" .planning/STATE.md` returns 0 — no bare/untagged "Benchmark decision gate:" rows remain after relabelling (every row has a sampler= tag).
  4. Both benchmark JSONs present: `ls results/power/benchmark_batched.json results/power/benchmark_batched_numpyro.json` exits 0.
  5. `git log --oneline -1 -- .planning/STATE.md` shows the docs(14.1) label-and-compare commit.
  </verify>
  <done>
  STATE.md has two disambiguated Benchmark decision gate rows (sampler=numpyro from 14.1-02, sampler=blackjax from 14.1-03); both JSONs on disk; numerical comparison recorded in SUMMARY; threshold tightening deferred to future planning.
  </done>
</task>

</tasks>

<verification>
1. Two labelled Benchmark decision gate rows in STATE.md (sampler=numpyro, sampler=blackjax).
2. results/power/benchmark_batched.json contains BlackJAX run; results/power/benchmark_batched_numpyro.json contains archived NumPyro baseline.
3. SUMMARY includes per_iteration_s and gpu_hours_per_chunk for both samplers so Phase 15 planning can read the comparison without re-running anything.
4. JIT gate thresholds in iteration.py apply_decision_gate are UNCHANGED (deferred).
</verification>

<success_criteria>
- Phase 15 readiness: authoritative per_iteration_s + gpu_hours_per_chunk for the production sampler (BlackJAX) exist on disk.
- STATE.md reflects both bench passes with disambiguated labels.
- Cluster filesystem and local filesystem are in sync via git auto-push block from 14.1-01.
</success_criteria>

<output>
After completion, create `.planning/phases/14-integration-gpu-benchmark/14.1-03-SUMMARY.md` with a numpyro-vs-blackjax comparison table: per_iteration_s, peak_vram_mb, mean_gpu_util_pct, decision, gpu_hours_per_chunk, jit_cold_s, jit_warm_s side by side. Include a recommendation: "Phase 15 should use <partition> based on BlackJAX decision=<value> and gpu_hours_per_chunk=<value>."
</output>
