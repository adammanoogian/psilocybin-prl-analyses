---
phase: 10-core-power-sweep
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - src/prl_hgf/power/iteration.py
  - src/prl_hgf/power/__init__.py
  - scripts/08_run_power_iteration.py
  - tests/test_power_iteration.py
autonomous: true

must_haves:
  truths:
    - "run_power_iteration returns a list of 3 dicts (one per contrast) each conforming to POWER_SCHEMA"
    - "The primary contrast (did_postdose) uses session_a=post_dose, session_b=baseline on omega_2 posterior means"
    - "BMS path fits both 2-level and 3-level models and computes XP for the 3-level model"
    - "bms_correct is True when XP for 3-level exceeds 0.75"
    - "scripts/08_run_power_iteration.py --task-id 0 --job-id test --dry-run still works (backward compat)"
    - "The full pipeline path writes 3 parquet files per task (one per contrast type)"
    - "recovery_r is computed for omega_2 via build_recovery_df + compute_recovery_metrics"
    - "idata for model 1 is deleted before fitting model 2 to limit peak memory"
  artifacts:
    - path: "src/prl_hgf/power/iteration.py"
      provides: "Core power iteration pipeline"
      exports: ["run_power_iteration", "build_arrays_from_sim"]
      min_lines: 120
    - path: "scripts/08_run_power_iteration.py"
      provides: "Updated entry point with full pipeline"
      contains: "run_power_iteration"
    - path: "src/prl_hgf/power/__init__.py"
      provides: "Updated exports"
      contains: "run_power_iteration"
    - path: "tests/test_power_iteration.py"
      provides: "Unit tests for iteration and array builder"
      min_lines: 60
  key_links:
    - from: "src/prl_hgf/power/iteration.py"
      to: "prl_hgf.simulation.batch.simulate_batch"
      via: "function call to generate synthetic participants"
      pattern: "simulate_batch"
    - from: "src/prl_hgf/power/iteration.py"
      to: "prl_hgf.fitting.batch.fit_batch"
      via: "function call with return_idata=True"
      pattern: "fit_batch.*return_idata=True"
    - from: "src/prl_hgf/power/iteration.py"
      to: "prl_hgf.power.contrasts.compute_all_contrasts"
      via: "BF computation on fit_df"
      pattern: "compute_all_contrasts"
    - from: "src/prl_hgf/power/iteration.py"
      to: "prl_hgf.analysis.bms.compute_subject_waic"
      via: "per-subject WAIC for BMS"
      pattern: "compute_subject_waic"
    - from: "src/prl_hgf/power/iteration.py"
      to: "prl_hgf.analysis.bms.run_group_bms"
      via: "random-effects BMS on ELPD matrix"
      pattern: "run_group_bms"
    - from: "scripts/08_run_power_iteration.py"
      to: "prl_hgf.power.iteration.run_power_iteration"
      via: "entry point calls core function"
      pattern: "run_power_iteration"
    - from: "scripts/08_run_power_iteration.py"
      to: "prl_hgf.power.grid.decode_task_id"
      via: "maps SLURM task ID to grid coordinates"
      pattern: "decode_task_id"
---

<objective>
Build the core power iteration pipeline that wires simulate -> fit -> BF -> BMS -> parquet for one SLURM task, and update the entry point script to replace the Phase 8 stub.

Purpose: This is the centerpiece of Phase 10. run_power_iteration() orchestrates the full simulate-fit-BF-BMS loop for one (N, d, iteration) cell. It calls existing pipeline functions (simulate_batch, fit_batch, compute_subject_waic, run_group_bms) and the new contrast functions from Plan 10-01, then returns 3 result dicts (one per contrast) ready for write_parquet_row. The entry point script decodes the SLURM task_id via Plan 10-02's grid functions and calls this pipeline.

Output: `power/iteration.py` with run_power_iteration(), updated entry point, updated __init__.py exports, and unit tests.
</objective>

<execution_context>
@C:\Users\aman0087\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\aman0087\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-core-power-sweep/10-RESEARCH.md

Key existing APIs (read these files for exact signatures):
@src/prl_hgf/simulation/batch.py — simulate_batch(config) -> DataFrame
@src/prl_hgf/fitting/batch.py — fit_batch(sim_df, model_name, ..., return_idata=True) -> (DataFrame, dict)
@src/prl_hgf/analysis/bms.py — compute_subject_waic(input_arr, obs_arr, choices_arr, idata, model_name) -> float; run_group_bms(elpd_matrix, model_names) -> dict
@src/prl_hgf/analysis/recovery.py — build_recovery_df(sim_df, fit_df, min_n=0) -> DataFrame; compute_recovery_metrics(recovery_df) -> DataFrame
@src/prl_hgf/power/config.py — make_power_config(base, n, d, seed) -> AnalysisConfig; PowerConfig
@src/prl_hgf/power/schema.py — write_parquet_row(row, path), POWER_SCHEMA (13 columns)
@src/prl_hgf/power/seeds.py — make_child_rng(master_seed, n_jobs, job_index) -> Generator

Prior plan outputs (from Plan 10-01 and 10-02):
- prl_hgf.power.contrasts.compute_all_contrasts(fit_df, parameter, bf_threshold) -> list[dict]
- prl_hgf.power.grid.decode_task_id(task_id, n_grid, d_grid, n_iter) -> (int, float, int)

CRITICAL PITFALL (Research Pitfall 1): Do NOT call compute_batch_waic with raw sim_df. 
simulate_batch outputs columns: participant_id, group, session, trial, cue_chosen, reward, true_*.
compute_batch_waic expects: reward_c0, reward_c1, reward_c2, observed_c0, observed_c1, observed_c2, choice.
Instead, call compute_subject_waic per participant after building arrays with the same 
logic as fit_batch._build_arrays (duplicate into build_arrays_from_sim since power/ does 
not modify existing modules).

CRITICAL PITFALL (Research Pitfall 6): BMS XP index.
Always pass model_names=["hgf_2level", "hgf_3level"] and extract xp[1] for 3-level.

Research Open Question 1: Write 3 rows per job (different sweep_type), one parquet file each.
Research Open Question 3: Group-stratified BMS is post-processing only (not computed per job).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create power/iteration.py with run_power_iteration and helpers</name>
  <files>src/prl_hgf/power/iteration.py, src/prl_hgf/power/__init__.py</files>
  <action>
Create `src/prl_hgf/power/iteration.py` with these components:

**1. `build_arrays_from_sim(subset: pd.DataFrame) -> tuple[np.ndarray, np.ndarray, np.ndarray]`**

Duplicates the logic of `fitting.batch._build_arrays` (do NOT import the private function):
- Takes a DataFrame subset for one participant-session, sorted by trial.
- Reads `cue_chosen` and `reward` columns.
- Returns `(input_data_arr, observed_arr, choices_arr)` with shapes (n_trials, 3), (n_trials, 3), (n_trials,).
- `input_data_arr[t, cue] = reward` and `observed_arr[t, cue] = 1` only for the chosen cue.

**2. `_compute_bms_power(sim_df, fit_df_3, fit_df_2, idata_3level, idata_2level) -> tuple[float, bool]`**

Private helper implementing Research Pattern 4. IMPORTANT: Uses incremental WAIC to limit peak memory (Research Pitfall 4 — at N=50 with 2 chains x 500 draws, both idata dicts simultaneously can reach 15-30 GB):

- Get unique (participant_id, group, session) keys from sim_df.
- **Phase A — 3-level WAIC (while idata_3level is in memory):**
  - For each key, filter sim_df to that participant-session, sort by trial.
  - Call `build_arrays_from_sim(subset)` to get input/observed/choices arrays.
  - Look up idata from `idata_3level` dict. If None (failed fit), skip this participant.
  - Call `compute_subject_waic(input_arr, obs_arr, choices_arr, idata, "hgf_3level")`.
  - Collect rows: {participant_id, model: "hgf_3level", elpd_waic}.
- **After Phase A completes, `del idata_3level` to free memory before Phase B.**
- **Phase B — 2-level WAIC (only idata_2level in memory now):**
  - Same loop over keys, but using `idata_2level` and model name "hgf_2level".
  - Collect rows: {participant_id, model: "hgf_2level", elpd_waic}.
- Combine Phase A and Phase B rows into a single DataFrame.
- Average elpd_waic across sessions per (participant_id, model).
- Pivot to (n_subjects, 2) matrix with columns [hgf_2level, hgf_3level]. Drop rows with NaN.
- If fewer than 3 subjects remain, return (0.5, False).
- Call `run_group_bms(matrix, ["hgf_2level", "hgf_3level"])`.
- Extract `xp_3level = float(bms_result["xp"][1])`. **Index 1 = hgf_3level** (critical).
- Return `(xp_3level, xp_3level > 0.75)`.

**3. `_extract_diagnostics(sim_df, fit_df) -> tuple[float, int, float]`**

Private helper:
- recovery_r: Call `build_recovery_df(sim_df, fit_df, exclude_flagged=True, min_n=0)` then `compute_recovery_metrics(recovery_df)`. Note: `compute_recovery_metrics` returns a **DataFrame with columns including 'parameter' and 'r'**. Filter to `parameter == "omega_2"` and extract `.iloc[0]["r"]`. Return NaN if omega_2 not found in the result or if recovery_df is empty.
- n_divergences: Sum `fit_df["n_divergences"]` if column exists, else 0. (Note: fit_batch doesn't produce this column currently — use 0 as placeholder and document.)
- mean_rhat: `fit_df["r_hat"].mean()` (NaN-safe with `nanmean`).
- Return `(recovery_r, n_divergences, mean_rhat)`.

**4. `run_power_iteration(base_config, n_per_group, effect_size_delta, iteration, child_seed, power_config, n_chains=2, n_draws=500, n_tune=500) -> list[dict]`**

Main orchestrator function:

```
Step 1: Build frozen config
    cfg = make_power_config(base_config, n_per_group, effect_size_delta, child_seed)

Step 2: Simulate
    sim_df = simulate_batch(cfg)

Step 3: Fit 3-level model (primary for BF path)
    fit_df_3, idata_3level = fit_batch(
        sim_df, "hgf_3level", return_idata=True,
        random_seed=child_seed, cores=1,
        n_chains=n_chains, n_draws=n_draws, n_tune=n_tune,
    )

Step 4: Compute contrasts + BF (uses fit_df_3 for omega_2 posterior means)
    contrast_results = compute_all_contrasts(
        fit_df_3, parameter="omega_2", bf_threshold=power_config.bf_threshold
    )

Step 5: Extract diagnostics (uses fit_df_3 + sim_df)
    recovery_r, n_divergences, mean_rhat = _extract_diagnostics(sim_df, fit_df_3)

Step 6: Fit 2-level model (for BMS path)
    fit_df_2, idata_2level = fit_batch(
        sim_df, "hgf_2level", return_idata=True,
        random_seed=child_seed + 1, cores=1,
        n_chains=n_chains, n_draws=n_draws, n_tune=n_tune,
    )

Step 7: Compute BMS power (incremental WAIC — deletes idata_3level internally)
    bms_xp, bms_correct = _compute_bms_power(
        sim_df, fit_df_3, fit_df_2, idata_3level, idata_2level
    )
    # After _compute_bms_power returns, idata_3level has been deleted internally.
    # Delete idata_2level here as well to release memory.
    del idata_2level

Step 8: Build 3 result dicts (one per contrast type)
    For each contrast in contrast_results:
        result = {
            "sweep_type": contrast["sweep_type"],
            "effect_size": effect_size_delta,
            "n_per_group": n_per_group,
            "trial_count": cfg.task.n_trials_total,
            "iteration": iteration,
            "parameter": "omega_2",
            "bf_value": contrast["bf_value"],
            "bf_exceeds": contrast["bf_exceeds"],
            "bms_xp": bms_xp,
            "bms_correct": bms_correct,
            "recovery_r": recovery_r,
            "n_divergences": n_divergences,
            "mean_rhat": mean_rhat,
        }

Return list of 3 dicts.
```

NOTE on step ordering: Steps 4-5 (BF contrasts + diagnostics) are computed from fit_df_3 BEFORE fitting the 2-level model in Step 6. This means idata_3level is still available for Step 7's BMS computation. The key memory optimization is inside _compute_bms_power which processes 3-level WAIC first, then deletes idata_3level, then processes 2-level WAIC.

Each dict conforms to POWER_SCHEMA (13 columns). The caller (entry point) writes each to a separate parquet file.

**5. Update `src/prl_hgf/power/__init__.py`**

Add to imports and __all__:
- `from prl_hgf.power.iteration import run_power_iteration, build_arrays_from_sim`
- `from prl_hgf.power.grid import decode_task_id, total_grid_size`
- `from prl_hgf.power.contrasts import compute_jzs_bf, compute_all_contrasts`

Use `from __future__ import annotations`. NumPy-style docstrings. Type hints with 3.10+ syntax throughout.

IMPORTANT: Do NOT import `_build_arrays` from `fitting.batch` — duplicate the logic in `build_arrays_from_sim` since the power/ package must not modify existing modules.
  </action>
  <verify>
Run `python -c "from prl_hgf.power.iteration import run_power_iteration, build_arrays_from_sim; print('imports OK')"`.

Run `python -c "from prl_hgf.power import run_power_iteration, decode_task_id, compute_all_contrasts; print('exports OK')"`.

Run `ruff check src/prl_hgf/power/iteration.py src/prl_hgf/power/__init__.py` — no errors.
  </verify>
  <done>
run_power_iteration orchestrates simulate -> fit (both models) -> BF contrasts -> BMS -> diagnostics and returns 3 POWER_SCHEMA-conforming dicts. build_arrays_from_sim duplicates _build_arrays logic without modifying fitting/batch.py. __init__.py exports all new Phase 10 symbols. Incremental WAIC in _compute_bms_power deletes idata_3level before processing 2-level, keeping peak memory to one model's idata at a time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update entry point script and write unit tests</name>
  <files>scripts/08_run_power_iteration.py, tests/test_power_iteration.py</files>
  <action>
**Update `scripts/08_run_power_iteration.py`** — Replace the stub `main()` body (the "full pipeline not yet implemented" print, lines 127-130) with the full pipeline. Key structural change:

1. **Move the old single-file `output_path` construction (line 104: `output_path = output_dir / f"job_{args.job_id}_task_{args.task_id:04d}.parquet"`) INSIDE the `if args.dry_run:` block.** The dry-run path still uses a single output file. Delete or do not use this old output_path variable anywhere outside dry-run. The full pipeline path builds its own per-contrast paths in a for-loop (see step 4 below).

2. After the dry-run block, add grid decode:
   ```python
   from prl_hgf.power.grid import decode_task_id
   n_per_group, effect_size_delta, iteration = decode_task_id(
       args.task_id,
       power_config.n_per_group_grid,
       power_config.effect_size_grid,
       power_config.n_iterations,
   )
   ```

3. Import and call the pipeline:
   ```python
   from prl_hgf.power.iteration import run_power_iteration
   results = run_power_iteration(
       base_config=base_config,
       n_per_group=n_per_group,
       effect_size_delta=effect_size_delta,
       iteration=iteration,
       child_seed=int(rng.integers(0, 2**31)),
       power_config=power_config,
       n_chains=args.fit_chains,
       n_draws=args.fit_draws,
       n_tune=args.fit_tune,
   )
   ```

4. Write 3 parquet files (one per contrast) using a NEW path scheme with sweep_type suffix:
   ```python
   for row in results:
       suffix = row["sweep_type"]
       out_path = output_dir / f"job_{args.job_id}_task_{args.task_id:04d}_{suffix}.parquet"
       write_parquet_row(row, out_path)
       print(f"Wrote: {out_path}")
   ```

5. Keep the existing `--dry-run` path completely unchanged (backward compat). The dry-run block has its own `output_path` and `return` so it never reaches the full pipeline code.

6. Add the necessary imports at the top (alongside existing ones):
   - `from prl_hgf.power.grid import decode_task_id`
   - `from prl_hgf.power.iteration import run_power_iteration`

**Unit tests** — Create `tests/test_power_iteration.py`:

1. `test_build_arrays_from_sim_matches_build_arrays`: Create a minimal sim_df subset (5 trials, cue_chosen and reward columns). Call `build_arrays_from_sim` and verify shapes: input_data_arr is (5, 3), observed_arr is (5, 3), choices_arr is (5,). Verify that for each trial, only the chosen cue has observed=1 and the correct reward value.

2. `test_build_arrays_from_sim_partial_feedback`: Create a 3-trial subset where different cues are chosen each trial. Verify that unchosen cues have input_data=0.0 and observed=0.

3. `test_run_power_iteration_return_structure` (mock-based):
   - Monkeypatch `simulate_batch` to return a minimal sim_df.
   - Monkeypatch `fit_batch` to return (a minimal fit_df with omega_2 means, a minimal idata_dict).
   - Monkeypatch `compute_all_contrasts` to return 3 known dicts.
   - Monkeypatch `_compute_bms_power` to return (0.8, True).
   - Monkeypatch `_extract_diagnostics` to return (0.9, 0, 1.01).
   - Call `run_power_iteration(...)`.
   - Assert result is a list of length 3.
   - Assert each dict has exactly the 13 POWER_SCHEMA keys.
   - Assert sweep_type values are ["did_postdose", "did_followup", "linear_trend"].

4. `test_run_power_iteration_schema_conformance` (mock-based):
   - Same mocking as test 3.
   - For each result dict, call `write_parquet_row(result, tmp_path / f"test_{i}.parquet")`.
   - Assert no ValueError is raised (schema validation passes).

5. `test_entry_point_dry_run_still_works`:
   - Import and call `main()` with `sys.argv = ["prog", "--task-id", "0", "--job-id", "test", "--dry-run", "--output-dir", str(tmp_path)]`.
   - Assert a parquet file is created in tmp_path.

Use pytest, monkeypatch for mocking, tmp_path for file output. Follow project test patterns.
  </action>
  <verify>
Run `python -m pytest tests/test_power_iteration.py -v` — all 5 tests pass.

Run `python scripts/08_run_power_iteration.py --task-id 0 --job-id test123 --dry-run --output-dir /tmp/power_test` — writes placeholder parquet (backward compat).

Run `ruff check scripts/08_run_power_iteration.py tests/test_power_iteration.py` — no errors.
  </verify>
  <done>
Entry point replaces the stub with full pipeline: decode_task_id -> run_power_iteration -> write 3 parquet files. Old single-file output_path is confined to the dry-run block; full pipeline uses per-contrast suffix paths. Dry-run mode preserved. 5 unit tests pass covering: array builder correctness, partial feedback, return structure with mocks, schema conformance with write_parquet_row, and dry-run backward compatibility.
  </done>
</task>

</tasks>

<verification>
- `python -c "from prl_hgf.power import run_power_iteration, decode_task_id, compute_all_contrasts"` — all imports succeed
- `python -m pytest tests/test_power_iteration.py tests/test_power_contrasts.py tests/test_power_grid.py -v` — all tests pass
- `python scripts/08_run_power_iteration.py --task-id 0 --job-id test --dry-run --output-dir /tmp/test_power` — dry run still works
- `ruff check src/prl_hgf/power/ scripts/08_run_power_iteration.py` — no lint errors
- Verify each result dict from run_power_iteration has exactly 13 keys matching POWER_SCHEMA
- Verify no stale `output_path` variable is used outside the dry-run block in the entry point
</verification>

<success_criteria>
1. run_power_iteration orchestrates full simulate->fit(3level)->BF->diagnostics->fit(2level)->BMS pipeline
2. Returns 3 result dicts (one per contrast: did_postdose, did_followup, linear_trend), each with 13 POWER_SCHEMA columns
3. BMS path uses compute_subject_waic per participant (not compute_batch_waic) to avoid column mismatch
4. _compute_bms_power uses incremental WAIC: processes 3-level first, deletes idata_3level, then processes 2-level (peak memory = one model's idata)
5. _extract_diagnostics correctly indexes compute_recovery_metrics DataFrame by parameter=="omega_2" column
6. build_arrays_from_sim duplicates _build_arrays logic without modifying fitting/batch.py
7. Entry point decodes task_id, calls run_power_iteration, writes 3 parquet files via per-contrast suffix paths
8. Old single-file output_path is inside dry-run block only; no dead code outside it
9. Dry-run mode backward compatible
10. Reduced MCMC settings (n_chains=2, n_draws=500, n_tune=500) passed through from CLI flags
11. All unit tests pass (5 in test_power_iteration + 10 in test_power_contrasts + 7 in test_power_grid)
</success_criteria>

<output>
After completion, create `.planning/phases/10-core-power-sweep/10-03-SUMMARY.md`
</output>
